#include <stdio.h>
#include <string.h>
int main(){
    char str[20];
int main(){
    char str[20] = "";
    strncat(str, "hello world", sizeof(str) - strlen(str) - 1);
    printf("%s", str);
    return 0;
}
    printf("%s", str);
    return 0;
}
